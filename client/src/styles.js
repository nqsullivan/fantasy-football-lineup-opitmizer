import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    grid: {
        justifyContent: 'space-between',
        alignItems: "stretch",
        spacing: "3",
    },
}));